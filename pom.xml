<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Organization ID -->
    <groupId>87b1fed7-baa4-4eac-ad1f-698a2b1ba331</groupId>
    <artifactId>first-project</artifactId>
    <version>1.0.0</version>
    <packaging>mule-application</packaging>

    <name>first-project</name>

    <!-- ===================== -->
    <!-- Properties -->
    <!-- ===================== -->
    <properties>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Mule Runtime -->
        <mule.version>4.11.0</mule.version>

        <!-- Mule Maven Plugin -->
        <mule.maven.plugin.version>4.7.0</mule.maven.plugin.version>

    </properties>

    <!-- ===================== -->
    <!-- Exchange Publish -->
    <!-- ===================== -->
    <distributionManagement>
        <repository>
            <id>anypoint-exchange-v3</id>
            <name>Anypoint Exchange</name>
            <url>
                https://maven.anypoint.mulesoft.com/api/v3/organizations/87b1fed7-baa4-4eac-ad1f-698a2b1ba331/maven
            </url>
        </repository>
    </distributionManagement>

    <!-- ===================== -->
    <!-- Build -->
    <!-- ===================== -->
    <build>

        <plugins>

            <!-- Mule Maven Plugin -->
            <plugin>

                <groupId>org.mule.tools.maven</groupId>
                <artifactId>mule-maven-plugin</artifactId>
                <version>${mule.maven.plugin.version}</version>
                <extensions>true</extensions>

                <configuration>

                    <!-- Exchange Deployment -->
                    <exchangeDeployment>
                        <uri>https://anypoint.mulesoft.com</uri>
                    </exchangeDeployment>

                    <!-- CloudHub 2.0 Deployment -->
                    <cloudhub2Deployment>

                        <uri>https://anypoint.mulesoft.com</uri>

                        <provider>MC</provider>

                        <environment>Sandbox</environment>

                        <target>Cloudhub-US-East-2</target>

                        <applicationName>first-project</applicationName>

                        <muleVersion>${mule.version}</muleVersion>

                        <releaseChannel>EDGE</releaseChannel>

                        <javaVersion>17</javaVersion>

                        <replicas>1</replicas>

                        <vCores>0.1</vCores>

                        <deploymentTimeout>100000</deploymentTimeout>

                        <skipDeploymentVerification>false</skipDeploymentVerification>

                        <!-- Connected App -->
                        <connectedAppClientId>${clientId}</connectedAppClientId>

                        <connectedAppClientSecret>${clientSecret}</connectedAppClientSecret>

                        <connectedAppGrantType>client_credentials</connectedAppGrantType>

                    </cloudhub2Deployment>

                </configuration>

            </plugin>

        </plugins>

    </build>

    <!-- ===================== -->
    <!-- Dependencies -->
    <!-- ===================== -->
    <dependencies>

        <!-- HTTP Connector -->
        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-http-connector</artifactId>
            <version>1.11.1</version>
            <classifier>mule-plugin</classifier>
        </dependency>

        <!-- APIKit Module -->
        <dependency>
            <groupId>org.mule.modules</groupId>
            <artifactId>mule-apikit-module</artifactId>
            <version>1.11.11</version>
            <classifier>mule-plugin</classifier>
        </dependency>

    </dependencies>

    <!-- ===================== -->
    <!-- Repositories -->
    <!-- ===================== -->
    <repositories>

        <repository>
            <id>anypoint-exchange-v3</id>
            <url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
        </repository>

        <repository>
            <id>mulesoft-releases</id>
            <url>https://repository.mulesoft.org/nexus/content/repositories/releases/</url>
        </repository>

        <repository>
            <id>mulesoft-public</id>
            <url>https://repository.mulesoft.org/nexus/content/repositories/public/</url>
        </repository>

    </repositories>

    <!-- ===================== -->
    <!-- Plugin Repositories -->
    <!-- ===================== -->
    <pluginRepositories>

        <pluginRepository>
            <id>mulesoft-releases</id>
            <url>https://repository.mulesoft.org/nexus/content/repositories/releases/</url>
        </pluginRepository>

        <pluginRepository>
            <id>mulesoft-public</id>
            <url>https://repository.mulesoft.org/nexus/content/repositories/public/</url>
        </pluginRepository>

    </pluginRepositories>

</project>